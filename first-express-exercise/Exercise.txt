1. Create a npm project and install Express.js (Nodemon if you want)
2. Create an Express.js app which funnels the request 
    throught 2 middleware functions that log 
    something to the console and returns one response
3. Handle requests to "/" and "/users"
   sucht that each request only has one handler/middlewear
   that does something with it.